# Supabase Configuration
# Copy this file to .env.local and fill in your actual values

# Get these from your Supabase project dashboard:
# Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Settings → API → anon public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Site URL for server-side redirects (recommended for production)
# If not set, the app will use window.location.origin on the client side
NEXT_PUBLIC_SITE_URL=https://swavleba.ge

# Optional: If you need server-side access
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here















